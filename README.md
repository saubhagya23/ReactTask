# ReactTask

To execute the web application, follow the below mentioned steps:
- clone the repository.
- npm i (this will install all the required modules.)
- npm run start (this will execute the application.)
